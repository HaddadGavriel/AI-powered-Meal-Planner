import { test,expect } from '@playwright/test';
test('demo user can navigate main meal planner areas',async({page})=>{await page.goto('/');await page.getByRole('button',{name:/sign in/i}).click();await expect(page.getByRole('heading',{name:/this week/i})).toBeVisible();await page.getByRole('link',{name:'Ingredients'}).click();await expect(page.getByRole('heading',{name:'Ingredients'})).toBeVisible();await page.getByRole('link',{name:'Recipes'}).click();await expect(page.getByText('Garden Vegetable Omelet')).toBeVisible();await page.getByRole('link',{name:'Meal plans'}).click();await expect(page.getByText('First week of August')).toBeVisible();await page.getByRole('link',{name:'Household'}).click();await expect(page.getByText('Pending invitations')).toBeVisible();});
